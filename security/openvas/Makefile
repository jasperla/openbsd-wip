# $OpenBSD$

    SUBDIR += libraries
    SUBDIR += scanner
    SUBDIR += cli
    SUBDIR += manager
#    SUBDIR += administrator
    SUBDIR += gsa
#    SUBDIR += gsd
# gnutls v3 no longer builds
#    SUBDIR += client

.include <bsd.port.subdir.mk>
