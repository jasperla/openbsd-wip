$OpenBSD$
--- CMakeLists.txt.orig	Fri Apr 15 14:30:03 2011
+++ CMakeLists.txt	Mon Apr 18 22:26:38 2011
@@ -147,7 +147,7 @@ endif (NOT INCLUDEDIR)
 #endif (NOT SYSCONFDIR)
 
 set (OPENVAS_DATA_DIR    "${DATADIR}/openvas")
-set (OPENVAS_STATE_DIR   "${LOCALSTATEDIR}/lib/openvas")
+set (OPENVAS_STATE_DIR   "${LOCALSTATEDIR}/openvas")
 set (OPENVAS_LOG_DIR     "${LOCALSTATEDIR}/log/openvas")
 set (OPENVAS_PID_DIR     "${LOCALSTATEDIR}/run")
 set (OPENVAS_SYSCONF_DIR "${SYSCONFDIR}/openvas")
@@ -176,7 +176,7 @@ pkg_check_modules (LIBOPENVAS REQUIRED libopenvas>=4.0
 pkg_check_modules (GNUTLS REQUIRED gnutls)
 pkg_check_modules (GLIB REQUIRED glib-2.0)
 pkg_check_modules (SQLITE3 REQUIRED sqlite3)
-pkg_check_modules (UUID REQUIRED uuid)
+pkg_check_modules (UUID REQUIRED e2fs-uuid)
 
 message (STATUS "Looking for pcap...")
 find_library (PCAP pcap)
@@ -220,10 +220,10 @@ execute_process (COMMAND pkg-config --libs sqlite3
   OUTPUT_VARIABLE SQLITE3_LDFLAGS
   OUTPUT_STRIP_TRAILING_WHITESPACE)
 
-execute_process (COMMAND pkg-config --cflags uuid
+execute_process (COMMAND pkg-config --cflags e2fs-uuid
   OUTPUT_VARIABLE UUID_CFLAGS
   OUTPUT_STRIP_TRAILING_WHITESPACE)
-execute_process (COMMAND pkg-config --libs uuid
+execute_process (COMMAND pkg-config --libs e2fs-uuid
   OUTPUT_VARIABLE UUID_LDFLAGS
   OUTPUT_STRIP_TRAILING_WHITESPACE)
 
@@ -240,7 +240,7 @@ configure_file (src/openvasmd_log_conf.cmake_in src/op
 
 ## Program
 
-set (HARDENING_FLAGS            "-Wformat -Wformat-security -O2 -D_FORTIFY_SOURCE=2 -fstack-protector -Wl,-z,relro -Wl,-z,now")
+set (HARDENING_FLAGS            "-Wformat -Wformat-security -O2 -D_FORTIFY_SOURCE=2 -fstack-protector")
 
 set (CMAKE_C_FLAGS_DEBUG        "${CMAKE_C_FLAGS_DEBUG}")
 set (CMAKE_C_FLAGS              "${CMAKE_C_FLAGS} ${HARDENING_FLAGS} -Wall -Werror -D_BSD_SOURCE -D_ISOC99_SOURCE -D_SVID_SOURCE")
@@ -260,7 +260,7 @@ enable_testing ()
 install (CODE "file (MAKE_DIRECTORY ${OPENVAS_STATE_DIR}/openvasmd/report_formats/)")
 
 install (FILES ${CMAKE_BINARY_DIR}/src/openvasmd_log.conf
-         DESTINATION ${OPENVAS_SYSCONF_DIR})
+         DESTINATION ${OPENVAS_DATA_DIR})
 
 install (FILES src/report_formats/CPE/generate
                src/report_formats/CPE/CPE.xsl
