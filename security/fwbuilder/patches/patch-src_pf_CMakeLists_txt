$OpenBSD$

Don't try linking against libdl
See https://github.com/fwbuilder/fwbuilder/pull/117

Index: src/pf/CMakeLists.txt
--- src/pf/CMakeLists.txt.orig
+++ src/pf/CMakeLists.txt
@@ -10,7 +10,7 @@ IF (WIN32)
 ENDIF()
 
 IF (UNIX)
-  target_link_libraries(fwb_pf c dl util)
+  target_link_libraries(fwb_pf c ${CMAKE_DL_LIBS} util)
 ENDIF()
 
 target_compile_options(fwb_pf PRIVATE ${CXX_DEFAULT_FLAGS})
