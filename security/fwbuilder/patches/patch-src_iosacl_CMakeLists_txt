$OpenBSD$

Don't try linking against libdl
See https://github.com/fwbuilder/fwbuilder/pull/117

Index: src/iosacl/CMakeLists.txt
--- src/iosacl/CMakeLists.txt.orig
+++ src/iosacl/CMakeLists.txt
@@ -10,7 +10,7 @@ IF (WIN32)
 ENDIF()
 
 IF (UNIX)
-  target_link_libraries(fwb_iosacl c dl util)
+  target_link_libraries(fwb_iosacl c ${CMAKE_DL_LIBS} util)
 ENDIF()
 
 target_compile_options(fwb_iosacl PRIVATE ${CXX_DEFAULT_FLAGS})
