$OpenBSD$

Don't try linking against libdl
See https://github.com/fwbuilder/fwbuilder/pull/117

Index: src/ipf/CMakeLists.txt
--- src/ipf/CMakeLists.txt.orig
+++ src/ipf/CMakeLists.txt
@@ -10,7 +10,7 @@ IF (WIN32)
 ENDIF()
 
 IF (UNIX)
-  target_link_libraries(fwb_ipf c dl util)
+  target_link_libraries(fwb_ipf c ${CMAKE_DL_LIBS} util)
 ENDIF()
 
 target_compile_options(fwb_ipf PRIVATE ${CXX_DEFAULT_FLAGS})
