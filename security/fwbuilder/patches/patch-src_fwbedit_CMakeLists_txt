$OpenBSD$

Don't try linking against libdl
See https://github.com/fwbuilder/fwbuilder/pull/117

Index: src/fwbedit/CMakeLists.txt
--- src/fwbedit/CMakeLists.txt.orig
+++ src/fwbedit/CMakeLists.txt
@@ -19,7 +19,7 @@ IF (WIN32)
 ENDIF()
 
 IF (UNIX)
-  target_link_libraries(fwbedit c dl util)
+  target_link_libraries(fwbedit c ${CMAKE_DL_LIBS} util)
 ENDIF()
 
 IF (NETSNMP_FOUND)
