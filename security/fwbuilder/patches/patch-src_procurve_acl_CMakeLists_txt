$OpenBSD$

Don't try linking against libdl
See https://github.com/fwbuilder/fwbuilder/pull/117

Index: src/procurve_acl/CMakeLists.txt
--- src/procurve_acl/CMakeLists.txt.orig
+++ src/procurve_acl/CMakeLists.txt
@@ -11,7 +11,7 @@ IF (WIN32)
 ENDIF()
 
 IF (UNIX)
-  target_link_libraries(fwb_procurve_acl c dl util)
+  target_link_libraries(fwb_procurve_acl c ${CMAKE_DL_LIBS} util)
 ENDIF()
 
 target_compile_options(fwb_procurve_acl PRIVATE ${CXX_DEFAULT_FLAGS})
