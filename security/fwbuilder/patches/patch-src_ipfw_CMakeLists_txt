$OpenBSD$

Don't try linking against libdl
See https://github.com/fwbuilder/fwbuilder/pull/117

Index: src/ipfw/CMakeLists.txt
--- src/ipfw/CMakeLists.txt.orig
+++ src/ipfw/CMakeLists.txt
@@ -10,7 +10,7 @@ IF (WIN32)
 ENDIF()
 
 IF (UNIX)
-  target_link_libraries(fwb_ipfw c dl util)
+  target_link_libraries(fwb_ipfw c ${CMAKE_DL_LIBS} util)
 ENDIF()
 
 target_compile_options(fwb_ipfw PRIVATE ${CXX_DEFAULT_FLAGS})
