$OpenBSD$

alloca(3) is in <stdlib.h>.

Index: xs/src/avrdude/libavrdude.h
--- xs/src/avrdude/libavrdude.h.orig
+++ xs/src/avrdude/libavrdude.h
@@ -948,7 +948,6 @@ int read_config(const char * file);
 #if defined(WIN32NATIVE)
 #  include <malloc.h>
 #else
-#  include <alloca.h>
 #endif
 
 
