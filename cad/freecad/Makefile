# $OpenBSD $

COMMENT=		general purpose feature-based CAD

GH_TAGNAME=		0.15
GH_ACCOUNT=		FreeCAD
GH_PROJECT=		FreeCAD

DISTNAME=		freecad-${GH_TAGNAME}

CATEGORIES=		cad

HOMEPAGE=		http://freecadweb.org/

# LGPL2+
PERMIT_PACKAGE_CDROM=	Yes

MODULES=		devel/cmake

BUILD_DEPENDS=		${RUN_DEPENDS}

.include <bsd.port.mk>
