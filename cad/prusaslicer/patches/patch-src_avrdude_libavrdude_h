$OpenBSD$

alloca(3) is in <stdlib.h>.

Index: src/avrdude/libavrdude.h
--- src/avrdude/libavrdude.h.orig
+++ src/avrdude/libavrdude.h
@@ -951,7 +951,6 @@ int read_config_builtin();
 #if defined(WIN32NATIVE)
 #  include <malloc.h>
 #else
-#  include <alloca.h>
 #endif
 
 
