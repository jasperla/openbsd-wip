Disable use of custom allocator (jemalloc not supported)
Index: crates/ruff_cli/Cargo.toml
--- crates/ruff_cli/Cargo.toml.orig
+++ crates/ruff_cli/Cargo.toml
@@ -65,9 +65,3 @@ ureq = { version = "2.5.0", features = [] }
 
 [package.metadata.maturin]
 name = "ruff"
-
-[target.'cfg(windows)'.dependencies]
-mimalloc = "0.1.29"
-
-[target.'cfg(all(not(windows), any(target_arch = "x86_64", target_arch = "aarch64", target_arch = "powerpc64")))'.dependencies]
-tikv-jemallocator = "0.5.0"
