COMMENT=		Easy wraper of Net::Curl modules

MODULES=		cpan
VER=			0.13
DISTNAME=		Net-Curl-Simple-${VER}
PKGNAME=		p5-${DISTNAME}
CATEGORIES=		devel perl5
CPAN_AUTHOR=		SPARKY

MAINTAINER=		Todd T. Fries <todd@fries.net>

# Artistic
PERMIT_PACKAGE=	Yes

RUN_DEPENDS+= ${BUILD_DEPENDS}
BUILD_DEPENDS+= devel/p5-Net-Curl

CONFIGURE_STYLE= perl

.include <bsd.port.mk>
