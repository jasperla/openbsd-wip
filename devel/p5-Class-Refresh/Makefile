COMMENT =	refresh your classes during runtime

DISTNAME =	Class-Refresh-0.07

CATEGORIES =	devel

# Perl
PERMIT_PACKAGE =	Yes

MODULES =		cpan

RUN_DEPENDS +=		devel/p5-Class-Load devel/p5-Class-Unload \
			devel/p5-Try-Tiny devel/p5-Devel-OverrideGlobalRequire

TEST_DEPENDS +=		devel/p5-Moose devel/p5-Test-Requires devel/p5-Test-Fatal

PKG_ARCH =		*

.include <bsd.port.mk>
