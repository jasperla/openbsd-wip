# $OpenBSD$

COMMENT =	produce tied (and other) separate but combined variables

MODULES =	cpan
DISTNAME =	IO-TieCombine-1.003
PKGNAME =	p5-${DISTNAME:S/-perl-/-/}
CATEGORIES =	devel

MAINTAINER =    Sergey Bronnikov <estetus@gmail.com>

# Perl
PERMIT_PACKAGE_CDROM =	Yes

.include <bsd.port.mk>
