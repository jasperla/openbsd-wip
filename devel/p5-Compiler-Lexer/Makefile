COMMENT =	lexical analyzer for Perl 5

DISTNAME =	Compiler-Lexer-0.23

CATEGORIES =	devel

# Perl
PERMIT_PACKAGE=	Yes

WANTLIB +=	${COMPILER_LIBCXX} c perl

BUILD_DEPENDS +=	devel/p5-Module-Build-XSUtil

MODULES =	cpan

CONFIGURE_STYLE =	modbuild

.include <bsd.port.mk>
