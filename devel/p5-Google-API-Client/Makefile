COMMENT=		client for Google APIs Discover Service

MODULES=		cpan
VER=			0.15
DISTNAME=		Google-API-Client-${VER}
PKGNAME=		p5-${DISTNAME}
CATEGORIES=		devel perl5
CPAN_AUTHOR=		SHIGETA

MAINTAINER=		Todd T. Fries <todd@fries.net>

# Artistic
PERMIT_PACKAGE=	Yes
PERMIT_DISTFILES=   Yes

#RUN_DEPENDS+= ${BUILD_DEPENDS}
#BUILD_DEPENDS+= 

CONFIGURE_STYLE= modbuild

.include <bsd.port.mk>
