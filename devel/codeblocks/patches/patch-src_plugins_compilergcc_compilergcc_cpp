Always use cb_console_runner from installed PREFIX.

Index: src/plugins/compilergcc/compilergcc.cpp
--- src/plugins/compilergcc/compilergcc.cpp.orig
+++ src/plugins/compilergcc/compilergcc.cpp
@@ -168,6 +168,7 @@ namespace
     PluginRegistrant<CompilerGCC> reg("Compiler");
 
     static const wxString strCONSOLE_RUNNER(platform::windows ? "cb_console_runner.exe" : "cb_console_runner");
+    static const wxString strCONSOLE_RUNNER(platform::windows ? "cb_console_runner.exe" : "${PREFIX}/bin/cb_console_runner");
     static const wxString strSLASH("/");
     static const wxString strSPACE(" ");
     static const wxString strQUOTE(platform::windows ? "\"" : "'");
@@ -2893,7 +2894,6 @@ void CompilerGCC::CalculateProjectDependencies(cbProje
 //                    Manager::Get()->GetMessageManager()->Log(m_PageIndex, "Adding dependency: %s", thisprj->GetTitle().c_str()));
                     deps.Add(idx);
                 }
-            }
         }
         else
             Manager::Get()->GetLogManager()->Log(wxString::Format(_("Circular dependency detected between \"%s\" and \"%s\". Skipping..."), prj->GetTitle(), thisprj->GetTitle()), m_PageIndex, Logger::warning);
