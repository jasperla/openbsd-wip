# $OpenBSD$

COMMENT =	easy-to-use daemon(izing) toolkit

MODULES =	cpan
DISTNAME =	Daemon-Daemonize-0.0052
CATEGORIES =	devel

MAINTAINER =	Sergey Bronnikov <estetus@gmail.com>

# Perl
PERMIT_PACKAGE_CDROM =	Yes

RUN_DEPENDS =	devel/p5-Path-Class \
		devel/p5-Sub-Exporter \
		devel/p5-Test-Most

BUILD_DEPENDS =	${RUN_DEPENDS}

.include <bsd.port.mk>
