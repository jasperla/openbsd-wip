# $OpenBSD$

COMMENT =	portably create pipe() or pipe-like handles
DISTNAME =	IO-Pipely-0.005
CATEGORIES =	devel

# Perl
PERMIT_PACKAGE_CDROM =	Yes

MODULES =	cpan

PKG_ARCH =	*

.include <bsd.port.mk>
