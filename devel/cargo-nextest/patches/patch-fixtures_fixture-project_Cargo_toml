Modify uuid crate version used for tests
NOK with 1.19.0 version

Index: fixtures/fixture-project/Cargo.toml
--- fixtures/fixture-project/Cargo.toml.orig
+++ fixtures/fixture-project/Cargo.toml
@@ -41,7 +41,7 @@ members = [
 ]
 
 [dependencies]
-uuid = "1.19.0"
+uuid = "1.18.1"
 
 [dev-dependencies]
 dylib-test = { path = "dylib-test" }
