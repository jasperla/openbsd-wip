# $OpenBSD $

COMMENT=	tolerance ranges for inexact numbers

MODULES=	cpan
DISTNAME=	Number-Tolerant-1.705
PKGNAME=	p5-${DISTNAME}
CATEGORIES=	devel

MAINTAINER =    Todd T. Fries <todd@OpenBSD.org>

# Perl
PERMIT_PACKAGE_CDROM=	Yes

RUN_DEPENDS=	devel/p5-Sub-Exporter

BUILD_DEPENDS=	${RUN_DEPENDS}

.include <bsd.port.mk>
