Index: renderer/Vulkan/vma.h
--- renderer/Vulkan/vma.h.orig
+++ renderer/Vulkan/vma.h
@@ -788,7 +788,9 @@ void* _aligned_alloc( size_t alignment, size_t size )
 	return NULL;
 }
 #elif !defined(_WIN32)
-#include <malloc.h> // for aligned_alloc()
+//#include <malloc.h> // for aligned_alloc()
+#include <sys/types.h>
+#include <sys/malloc.h>
 #endif
 
 // Normal assert to check for programmer's errors, especially in Debug configuration.
