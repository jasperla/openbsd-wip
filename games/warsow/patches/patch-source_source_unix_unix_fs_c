$OpenBSD$

Index: source/source/unix/unix_fs.c
--- source/source/unix/unix_fs.c.orig
+++ source/source/unix/unix_fs.c
@@ -29,6 +29,7 @@ Foundation, Inc., 59 Temple Place - Suite 330, Boston,
 #include <linux/limits.h>
 #endif
 
+#include <stdlib.h>
 #include <unistd.h>
 #include <sys/stat.h>
 #include <sys/mman.h>
@@ -37,8 +38,8 @@ Foundation, Inc., 59 Temple Place - Suite 330, Boston,
 #include "../android/android_sys.h"
 #endif
 
-// Mac OS X and FreeBSD don't know the readdir64 and dirent64
-#if ( defined (__FreeBSD__) || defined (__ANDROID__) || !defined(_LARGEFILE64_SOURCE) )
+// Mac OS X, FreeBSD and OpenBSD don't know the readdir64 and dirent64
+#if ( defined (__FreeBSD__) || defined (__OpenBSD__) || defined (__ANDROID__) || !defined(_LARGEFILE64_SOURCE) )
 #define readdir64 readdir
 #define dirent64 dirent
 #endif
