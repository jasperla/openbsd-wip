$OpenBSD$

strlcpy

Index: codemp/ui/ui_shared.c
--- codemp/ui/ui_shared.c.orig
+++ codemp/ui/ui_shared.c
@@ -246,7 +246,7 @@ const char *String_Alloc(const char *p) {
 	len = strlen(p);
 	if (len + strPoolIndex + 1 < STRING_POOL_SIZE) {
 		int ph = strPoolIndex;
-		strcpy(&strPool[strPoolIndex], p);
+		strlcpy(&strPool[strPoolIndex], p, sizeof(&strPool[strPoolIndex]));
 		strPoolIndex += len + 1;
 
 		str = strHandle[hash];
@@ -7119,7 +7119,7 @@ qboolean ItemParse_asset_model_go( itemDef_t *item, co
 
 					if ( slash )
 					{ //If this isn't true the gla path must be messed up somehow.
-						strcpy(slash, "/animation.cfg");
+						strlcpy(slash, "/animation.cfg", sizeof(slash));
 
 						animIndex = UI_ParseAnimationFile(GLAName, NULL, qfalse);
 						if (animIndex != -1)
