$OpenBSD$

strlcat

Index: tools/Assimilate/AssimilateDoc.cpp
--- tools/Assimilate/AssimilateDoc.cpp.orig
+++ tools/Assimilate/AssimilateDoc.cpp
@@ -1266,7 +1266,7 @@ void R_CheckCARs( LPCSTR psDir, int iScanDepth, LPCSTR
 		iCARsFound++;
 	/*		char	tgain[MAX_OSPATH];
 			sprintf(tgain,"%s\\%s", psDir, sysFiles[i]);
-			strcpy( altname, tgain );
+			strlcpy( altname, tgain , sizeof(altname));
 			len = strlen( altname );
 			altname[len-3] = 'j';
 			altname[len-2] = 'p';
@@ -2799,7 +2799,7 @@ void CAssimilateDoc::OnEditBuildDependant()
 		if (psCurrentGLAName)
 		{
 			char sCurrentGLAName[1024];
-			strcpy(sCurrentGLAName,psCurrentGLAName);
+			strlcpy(sCurrentGLAName,psCurrentGLAName, sizeof(sCurrentGLAName));
 			strMustContainThisGLA = sCurrentGLAName;
 			CBuildAll dlgBuildAll(strInitialBuildPath, gbPreValidate);
 
@@ -3314,7 +3314,7 @@ bool RunApp(LPCSTR psAppCommand)
 	char sBatchFilename[512];
 	
 	GetTempPath(sizeof(sBatchFilename), sBatchFilename);
-	strcat(sBatchFilename,"~temp.bat");
+	strlcat(sBatchFilename,"~temp.bat", sizeof(sBatchFilename));
 
 	FILE *handle = fopen(sBatchFilename,"wt");
 	fprintf(handle,strExec);
