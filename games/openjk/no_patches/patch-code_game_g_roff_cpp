$OpenBSD$

strlcpy

Index: code/game/g_roff.cpp
--- code/game/g_roff.cpp.orig
+++ code/game/g_roff.cpp
@@ -172,7 +172,7 @@ defaultoffsetposition:
 			}
 			teststr[r2] = '\0';
 
-			strcpy(argument, teststr);
+			strlcpy(argument, teststr, sizeof(argument));
 		}
 
 		objectID = G_EffectIndex(argument);
