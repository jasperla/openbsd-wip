$OpenBSD$

strlcpy

Index: codemp/cgame/cg_main.c
--- codemp/cgame/cg_main.c.orig
+++ codemp/cgame/cg_main.c
@@ -1403,7 +1403,7 @@ Ghoul2 Insert End
 			break;
 		}
 
-		strcpy(modelName, cModelName);
+		strlcpy(modelName, cModelName, sizeof(modelName));
 		if (strstr(modelName, ".glm") || modelName[0] == '$')
 		{ //Check to see if it has a custom skin attached.
 			CG_HandleAppendedSkin(modelName);
@@ -2875,7 +2875,7 @@ static qboolean CG_IncomingConsoleCommand( void ) {
 	}
 	else if ( strstr( icc->conCommand, "blah" ) )
 	{ //any command containing the string "blah" is redirected to "quit"
-		strcpy( icc->conCommand, "quit" );
+		strlcpy( icc->conCommand, "quit" , sizeof(icc->conCommand));
 		return qfalse;
 	}
 #endif
