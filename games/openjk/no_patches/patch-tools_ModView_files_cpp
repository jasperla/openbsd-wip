$OpenBSD$

strlcpy

Index: tools/ModView/files.cpp
--- tools/ModView/files.cpp.orig
+++ tools/ModView/files.cpp
@@ -182,7 +182,7 @@ char *CopyString( const char *in ) {
 	char	*out;
 	
 	out = (char *)S_Malloc (strlen(in)+1);
-	strcpy (out, in);
+	strlcpy (out, in, sizeof(out));
 	return out;
 }
 
