$OpenBSD$

strlcpy

Index: codemp/qcommon/cvar.cpp
--- codemp/qcommon/cvar.cpp.orig
+++ codemp/qcommon/cvar.cpp
@@ -1624,7 +1624,7 @@ static void Cvar_Realloc(char **string, char *memPool,
 	if(string && *string)
 	{
 		char *temp = memPool + memPoolUsed;
-		strcpy(temp, *string);
+		strlcpy(temp, *string, sizeof(temp));
 		memPoolUsed += strlen(*string) + 1;
 		Cvar_FreeString(*string);
 		*string = temp;
