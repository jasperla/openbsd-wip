$OpenBSD$

strlcat

Index: tools/lipsyncthing/main.cpp
--- tools/lipsyncthing/main.cpp.orig
+++ tools/lipsyncthing/main.cpp
@@ -369,7 +369,7 @@ static void WriteDataToFile(byte* data, const char* in
 	// create an output file based on the infile
 	outfile = (char*)malloc(strlen(infile)+1);
 	StripExtension(infile,outfile);
-	strcat(outfile,".lip");
+	strlcat(outfile,".lip", sizeof(outfile));
 
 	// open the outfile
 	out = fopen(outfile, "wb");
@@ -417,4 +417,4 @@ int main(int argc, const char** argv)
 	}
 
 	return 0;
-}
\ No newline at end of file
+}
