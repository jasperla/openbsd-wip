$OpenBSD$

strlcat etc

Index: codemp/rd-dedicated/G2_bones.cpp
--- codemp/rd-dedicated/G2_bones.cpp.orig
+++ codemp/rd-dedicated/G2_bones.cpp
@@ -3157,12 +3157,12 @@ void G2_RagPrintMatrix(mdxaBone_t *mat)
 		int o = 0;
 		while (o < 4)
 		{
-            strcat(x, va("%f ", mat->matrix[n][o]));
+            strlcat(x, va("%f ", mat->matrix[n][o]), sizeof(x));
 			o++;
 		}
 		n++;
 	}
-	strcat(x, "\n");
+	strlcat(x, "\n", sizeof(x));
 	Com_Printf(x);
 }
 #endif
