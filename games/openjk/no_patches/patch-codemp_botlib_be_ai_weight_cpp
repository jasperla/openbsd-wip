$OpenBSD$

strlcpy

Index: codemp/botlib/be_ai_weight.cpp
--- codemp/botlib/be_ai_weight.cpp.orig
+++ codemp/botlib/be_ai_weight.cpp
@@ -358,7 +358,7 @@ weightconfig_t *ReadWeightConfig(char *filename)
 			} //end if
 			StripDoubleQuotes(token.string);
 			config->weights[config->numweights].name = (char *) GetClearedMemory(strlen(token.string) + 1);
-			strcpy(config->weights[config->numweights].name, token.string);
+			strlcpy(config->weights[config->numweights].name, token.string, sizeof(config->weights[config->numweights].name));
 			if (!PC_ExpectAnyToken(source, &token))
 			{
 				FreeWeightConfig(config);
