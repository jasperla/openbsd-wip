$OpenBSD$

strlcpy

Index: codemp/client/FxScheduler.cpp
--- codemp/client/FxScheduler.cpp.orig
+++ codemp/client/FxScheduler.cpp
@@ -162,7 +162,7 @@ SEffectTemplate &SEffectTemplate::operator=(const SEff
 {
 	mCopy = true;
 
-	strcpy( mEffectName, that.mEffectName );
+	strlcpy( mEffectName, that.mEffectName , sizeof(mEffectName));
 
 	mPrimitiveCount = that.mPrimitiveCount;
 
@@ -519,7 +519,7 @@ SEffectTemplate *CFxScheduler::GetNewEffectTemplate( i
 			if ( file )
 			{
 				mEffectIDs[file] = i;
-				strcpy( effect->mEffectName, file );
+				strlcpy( effect->mEffectName, file , sizeof(effect->mEffectName));
 			}
 
 			effect->mInUse = true;
