$OpenBSD$

strlcpy

Index: codemp/game/bg_misc.c
--- codemp/game/bg_misc.c.orig
+++ codemp/game/bg_misc.c
@@ -3163,7 +3163,7 @@ void BG_TempFree( int size )
 char *BG_StringAlloc ( const char *source )
 {
 	char *dest = (char*)BG_Alloc( strlen ( source ) + 1 );
-	strcpy( dest, source );
+	strlcpy( dest, source , sizeof(dest));
 	return dest;
 }
 
