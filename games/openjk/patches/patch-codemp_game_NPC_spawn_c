$OpenBSD$

strlcpy

Index: codemp/game/NPC_spawn.c
--- codemp/game/NPC_spawn.c.orig
+++ codemp/game/NPC_spawn.c
@@ -2117,7 +2117,7 @@ qboolean NPC_VehiclePrecache( gentity_t *spawner )
 				char *slash = Q_strrchr( GLAName, '/' );
 				if ( slash )
 				{
-					strcpy(slash, "/animation.cfg");
+					strlcpy(slash, "/animation.cfg", sizeof(slash));
 
 					BG_ParseAnimationFile(GLAName, NULL, qfalse);
 				}
