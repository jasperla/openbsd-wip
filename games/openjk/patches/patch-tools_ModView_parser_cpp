$OpenBSD$

strlcpy

Index: tools/ModView/parser.cpp
--- tools/ModView/parser.cpp.orig
+++ tools/ModView/parser.cpp
@@ -49,7 +49,7 @@ bool Parser_Load(LPCSTR psFullPathedFilename, MappedSt
 					str.TrimLeft();
 					str.TrimRight();
 
-			strcpy(sLine,str);
+			strlcpy(sLine,str, sizeof(sLine));
 
 			if (!bSkippingBlock)
 			{
