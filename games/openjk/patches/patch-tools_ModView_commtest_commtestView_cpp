$OpenBSD$

strlcpy

Index: tools/ModView/commtest/commtestView.cpp
--- tools/ModView/commtest/commtestView.cpp.orig
+++ tools/ModView/commtest/commtestView.cpp
@@ -363,7 +363,7 @@ char *InputLoadFileName(LPCSTR psInitialLoadName, LPCS
 
 	FileDlg.m_ofn.lpstrInitialDir = (LPCSTR) strInitialDir;
 	FileDlg.m_ofn.lpstrTitle=psCaption;	// Load Editor Model";  
-	strcpy(name,psInitialLoadName);
+	strlcpy(name,psInitialLoadName, sizeof(name));
 	FileDlg.m_ofn.lpstrFile=name;
 		
    	if (FileDlg.DoModal() == IDOK)
