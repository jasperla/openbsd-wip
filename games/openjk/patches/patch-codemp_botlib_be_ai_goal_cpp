$OpenBSD$

strlcpy

Index: codemp/botlib/be_ai_goal.cpp
--- codemp/botlib/be_ai_goal.cpp.orig
+++ codemp/botlib/be_ai_goal.cpp
@@ -696,7 +696,7 @@ void BotGoalName(int number, char *name, int size)
 			return;
 		} //end for
 	} //end for
-	strcpy(name, "");
+	strlcpy(name, "", sizeof(name));
 	return;
 } //end of the function BotGoalName
 //===========================================================================
