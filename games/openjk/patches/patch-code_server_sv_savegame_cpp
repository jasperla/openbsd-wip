$OpenBSD$

strlcat

Index: code/server/sv_savegame.cpp
--- code/server/sv_savegame.cpp.orig
+++ code/server/sv_savegame.cpp
@@ -97,7 +97,7 @@ static const char *GetString_FailedToOpenSaveGame(cons
 {
 	static char sTemp[256];
 
-	strcpy(sTemp,S_COLOR_RED);
+	strlcpy(sTemp,S_COLOR_RED, sizeof(sTemp));
 
 #ifdef JK2_MODE
 	const char *psReference = bOpen ? "MENUS3_FAILED_TO_OPEN_SAVEGAME" : "MENUS3_FAILED_TO_CREATE_SAVEGAME";
@@ -105,7 +105,7 @@ static const char *GetString_FailedToOpenSaveGame(cons
 	const char *psReference = bOpen ? "MENUS_FAILED_TO_OPEN_SAVEGAME" : "MENUS3_FAILED_TO_CREATE_SAVEGAME";
 #endif
 	Q_strncpyz(sTemp + strlen(sTemp), va( SE_GetString(psReference), psFilename),sizeof(sTemp));
-	strcat(sTemp,"\n");
+	strlcat(sTemp,"\n", sizeof(sTemp));
 	return sTemp;
 }
 
