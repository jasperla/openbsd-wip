$OpenBSD$

strlcpy

Index: codemp/game/bg_saberLoad.c
--- codemp/game/bg_saberLoad.c.orig
+++ codemp/game/bg_saberLoad.c
@@ -2156,7 +2156,7 @@ qboolean WP_SaberParseParm( const char *saberName, con
 			{
 				continue;
 			}
-			strcpy( saberData, value );
+			strlcpy( saberData, value , sizeof(saberData));
 			return qtrue;
 		}
 
@@ -2193,7 +2193,7 @@ void WP_RemoveSaber( saberInfo_t *sabers, int saberNum
 	//reset everything for this saber just in case
 	WP_SaberSetDefaults( &sabers[saberNum] );
 
-	strcpy(sabers[saberNum].name, "none");
+	strlcpy(sabers[saberNum].name, "none", sizeof(sabers[saberNum].name));
 	sabers[saberNum].model[0] = 0;
 
 	//ent->client->ps.dualSabers = qfalse;
