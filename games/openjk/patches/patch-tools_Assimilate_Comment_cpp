$OpenBSD$

strlcpy

Index: tools/Assimilate/Comment.cpp
--- tools/Assimilate/Comment.cpp.orig
+++ tools/Assimilate/Comment.cpp
@@ -41,7 +41,7 @@ void CComment::SetComment(LPCTSTR comment)
 	else
 	{
 		m_comment = (char*)malloc(strlen(comment) + 1);
-		strcpy(m_comment, comment);
+		strlcpy(m_comment, comment, sizeof(m_comment));
 	}
 }
 
