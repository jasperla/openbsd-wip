$OpenBSD$

strlcpy

Index: shared/sys/sys_win32.cpp
--- shared/sys/sys_win32.cpp.orig
+++ shared/sys/sys_win32.cpp
@@ -141,11 +141,11 @@ char *Sys_GetCurrentUser( void )
 	DWORD size = sizeof( s_userName );
 
 	if ( !GetUserName( s_userName, &size ) )
-		strcpy( s_userName, "player" );
+		strlcpy( s_userName, "player" , sizeof(s_userName));
 
 	if ( !s_userName[0] )
 	{
-		strcpy( s_userName, "player" );
+		strlcpy( s_userName, "player" , sizeof(s_userName));
 	}
 
 	return s_userName;
