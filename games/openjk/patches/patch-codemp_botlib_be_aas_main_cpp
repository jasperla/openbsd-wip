$OpenBSD$

strlcpy

Index: codemp/botlib/be_aas_main.cpp
--- codemp/botlib/be_aas_main.cpp.orig
+++ codemp/botlib/be_aas_main.cpp
@@ -228,7 +228,7 @@ int AAS_LoadFiles(const char *mapname)
 	char aasfile[MAX_PATH];
 //	char bspfile[MAX_PATH];
 
-	strcpy(aasworld.mapname, mapname);
+	strlcpy(aasworld.mapname, mapname, sizeof(aasworld.mapname));
 	//NOTE: first reset the entity links into the AAS areas and BSP leaves
 	// the AAS link heap and BSP link heap are reset after respectively the
 	// AAS file and BSP file are loaded
