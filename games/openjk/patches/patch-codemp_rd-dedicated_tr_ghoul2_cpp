$OpenBSD$

strlcpy

Index: codemp/rd-dedicated/tr_ghoul2.cpp
--- codemp/rd-dedicated/tr_ghoul2.cpp.orig
+++ codemp/rd-dedicated/tr_ghoul2.cpp
@@ -2742,20 +2742,20 @@ void G2_GetBoltMatrixLow(CGhoul2Info &ghoul2,int boltN
 
 		if (ghoul2.currentModel)
 		{
-			strcpy(mName, ghoul2.currentModel->name);
+			strlcpy(mName, ghoul2.currentModel->name, sizeof(mName));
 		}
 		else
 		{
-			strcpy(mName, "NULL!");
+			strlcpy(mName, "NULL!", sizeof(mName));
 		}
 
 		if (ghoul2.mFileName && ghoul2.mFileName[0])
 		{
-			strcpy(fName, ghoul2.mFileName);
+			strlcpy(fName, ghoul2.mFileName, sizeof(fName));
 		}
 		else
 		{
-			strcpy(fName, "None?!");
+			strlcpy(fName, "None?!", sizeof(fName));
 		}
 
 		Com_Error(ERR_DROP, "Write down or save this error message, show it to Rich\nBad bolt index on model %s (filename %s), index %i boltlink %i\n", mName, fName, boltNum, bLink);
