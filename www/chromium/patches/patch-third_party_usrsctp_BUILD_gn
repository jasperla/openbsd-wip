$OpenBSD$

Index: third_party/usrsctp/BUILD.gn
--- third_party/usrsctp/BUILD.gn.orig
+++ third_party/usrsctp/BUILD.gn
@@ -139,8 +139,10 @@ static_library("usrsctp") {
     "-UINET6",
   ]
 
-  if (is_linux || is_chromeos || is_android) {
+  if ((is_linux || is_chromeos || is_android) && !is_openbsd) {
     defines += [ "__Userspace_os_Linux" ]
+  } else if (is_openbsd) {
+    defines += [ "__Userspace_os_OpenBSD" ]
   } else if (is_apple) {
     defines += [
       "HAVE_SA_LEN",
