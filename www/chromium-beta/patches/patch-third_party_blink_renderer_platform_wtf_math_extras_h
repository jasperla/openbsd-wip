$OpenBSD$

XXX LLVM 13 constexpr

Index: third_party/blink/renderer/platform/wtf/math_extras.h
--- third_party/blink/renderer/platform/wtf/math_extras.h.orig
+++ third_party/blink/renderer/platform/wtf/math_extras.h
@@ -127,6 +127,8 @@ constexpr float Grad2turn(float g) {
   return g * (1.0f / 400.0f);
 }
 
+#pragma clang diagnostic push
+#pragma clang diagnostic ignored "-Winvalid-constexpr"
 constexpr double RoundHalfTowardsPositiveInfinity(double value) {
   return std::floor(value + 0.5);
 }
@@ -134,6 +136,7 @@ constexpr double RoundHalfTowardsPositiveInfinity(doub
 constexpr float RoundHalfTowardsPositiveInfinity(float value) {
   return std::floor(value + 0.5f);
 }
+#pragma clang diagnostic pop
 
 // ClampTo() is implemented by templated helper classes (to allow for partial
 // template specialization) as well as several helper functions.
