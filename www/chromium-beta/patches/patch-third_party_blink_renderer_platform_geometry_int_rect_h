$OpenBSD$

XXX LLVM 13 constexpr

Index: third_party/blink/renderer/platform/geometry/int_rect.h
--- third_party/blink/renderer/platform/geometry/int_rect.h.orig
+++ third_party/blink/renderer/platform/geometry/int_rect.h
@@ -244,9 +244,12 @@ constexpr IntRect SaturatedRect(const IntRect& r) {
                  base::ClampAdd(r.y(), r.height()) - r.y());
 }
 
+#pragma clang diagnostic push
+#pragma clang diagnostic ignored "-Winvalid-constexpr"
 constexpr bool operator==(const IntRect& a, const IntRect& b) {
   return a.origin() == b.origin() && a.size() == b.size();
 }
+#pragma clang diagnostic pop
 
 constexpr bool operator!=(const IntRect& a, const IntRect& b) {
   return !(a == b);
