$OpenBSD$
--- chrome/browser/ui/fullscreen/exclusive_access_bubble.cc.orig.port	Sat Dec 27 15:37:24 2014
+++ chrome/browser/ui/fullscreen/exclusive_access_bubble.cc	Sat Dec 27 15:37:31 2014
@@ -18,7 +18,7 @@
 
 // NOTE(koz): Linux doesn't use the thick shadowed border, so we add padding
 // here.
-#if defined(OS_LINUX)
+#if defined(OS_LINUX) || defined(OS_BSD)
 const int ExclusiveAccessBubble::kPaddingPx = 8;
 #else
 const int ExclusiveAccessBubble::kPaddingPx = 15;
