$OpenBSD$
--- third_party/WebKit/Source/platform/heap/ThreadState.h.orig.port	Tue Jan  6 02:03:28 2015
+++ third_party/WebKit/Source/platform/heap/ThreadState.h	Wed Jan  7 21:41:25 2015
@@ -367,7 +367,7 @@ class PLATFORM_EXPORT ThreadState { (public)
 
     static ThreadState* current()
     {
-#if defined(__GLIBC__) || OS(ANDROID) || OS(FREEBSD)
+#if defined(__GLIBC__) || OS(ANDROID) || OS(FREEBSD) || OS(OPENBSD)
         // TLS lookup is fast in these platforms.
         return **s_threadSpecific;
 #else
