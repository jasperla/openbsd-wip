To completely deinstall the mod_wsgi package you should edit

/var/www/conf/httpd.conf

and remove the line that starts with:

LoadModule wsgi_module

Do not do this if you plan on re-installing the mod_wsgi
package at some future time.
