$OpenBSD$
--- base/process_util_openbsd.cc.orig	Mon Nov  7 12:21:41 2011
+++ base/process_util_openbsd.cc	Tue Nov  8 18:40:09 2011
@@ -107,7 +107,7 @@ ProcessIterator::ProcessIterator(const ProcessFilter* 
   } while (!done && (try_num++ < max_tries));
 
   if (!done) {
-    DDLOG(ERROR) << "failed to collect the process list in a few tries";
+    DLOG(ERROR) << "failed to collect the process list in a few tries";
     kinfo_procs_.resize(0);
   }
 }
