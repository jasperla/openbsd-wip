$OpenBSD$
--- chrome/common/multi_process_lock_win.cc.orig	Mon Nov  7 12:22:13 2011
+++ chrome/common/multi_process_lock_win.cc	Tue Nov  8 18:40:08 2011
@@ -24,9 +24,9 @@ class MultiProcessLockWin : public MultiProcessLock {
       return true;
     }
 
-    if (name_.length() > MULTI_PROCESS_LOCK_NAME_MAX_LEN) {
+    if (name_.length() >= MAX_PATH) {
       DLOG(ERROR) << "Socket name too long (" << name_.length()
-                  << " > " << MULTI_PROCESS_LOCK_NAME_MAX_LEN << ") - "
+                  << " >= " << MAX_PATH << ") - "
                   << name_;
       return false;
     }
