$OpenBSD$
--- third_party/leveldatabase/env_chromium.cc.orig	Thu Aug 25 09:43:31 2011
+++ third_party/leveldatabase/env_chromium.cc	Thu Aug 25 09:43:57 2011
@@ -30,7 +30,7 @@
 #include "base/win/win_util.h"
 #endif
 
-#if defined(OS_MACOSX) || defined(OS_WIN)
+#if defined(OS_MACOSX) || defined(OS_WIN) || defined(OS_OPENBSD)
 // The following are glibc-specific
 namespace {
 
@@ -330,7 +330,7 @@ class ChromiumEnv : public Env {
 
   virtual Status GetFileSize(const std::string& fname, uint64_t* size) {
     Status s;
-    int64_t signed_size;
+    int64 signed_size;
     if (!::file_util::GetFileSize(CreateFilePath(fname), &signed_size)) {
       *size = 0;
       s = Status::IOError(fname, "Could not determine file size.");
