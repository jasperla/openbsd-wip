* "make test" seems to hang at some point.
* Manpages need checking against mandoc/groff.
* Some completions may be wrong.  BSD- (jot, etc.) and OpenBSD-specific
  completions (rcctl, pkg_add) should probably be added.
