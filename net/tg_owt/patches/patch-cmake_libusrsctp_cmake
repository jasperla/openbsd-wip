$OpenBSD$

Identify as OpenBSD avoid macro redifinitions

Index: cmake/libusrsctp.cmake
--- cmake/libusrsctp.cmake.orig
+++ cmake/libusrsctp.cmake
@@ -34,7 +34,7 @@ elseif (APPLE)
 else()
     target_compile_definitions(libusrsctp
     PRIVATE
-        __Userspace_os_Linux
+        __Userspace_os_OpenBSD
     )
 endif()
 
