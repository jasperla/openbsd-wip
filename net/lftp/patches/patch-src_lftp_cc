$OpenBSD$

add pledge(2)

--- src/lftp.cc.orig	Thu Dec 10 13:17:48 2015
+++ src/lftp.cc	Sun Jan 17 00:51:58 2016
@@ -537,6 +537,14 @@ char *program_name;
 
 int   main(int argc,char **argv)
 {
+#ifdef __OpenBSD__
+    if (pledge("stdio rpath wpath cpath fattr flock inet dns getpw unix proc id tty", NULL) == -1)
+    {
+        fprintf(stderr, "lftp: pledge\n");
+        exit(1);
+    }
+#endif
+
    program_name=argv[0];
 
 #ifdef SOCKS4
