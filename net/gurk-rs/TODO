- Update to 0.4: (done)

The port looks good now, but it's crashing for
me after pairing with the QR code.

Notes:
    Vendored crates:
        curve25519-dalek
        libsignal-protocol
        libsignal-service
        libsignal-service-hyper
        poksho
        presage
        zkgroup
        notify-rust (patched with files/notification.rs.patch)
