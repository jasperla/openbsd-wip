l2tpd is an implementation of the layer two tunneling protocol. It
works in userspace completely. l2tpd works by opening a pseudo-tty for
communicating with pppd.  Although l2tpd was written for Linux, the
current version should be highly portable to other UNIX's supported by
pppd.
