$OpenBSD$
--- Makefile.unix-install.orig	Sun Jul 28 01:23:52 2013
+++ Makefile.unix-install	Sun Jul 28 01:27:30 2013
@@ -50,10 +50,8 @@ install-etc: install-etc-dir
 	done;
 
 install-man:
-	$(INSTALL_BIN) -d $(MANDIR1)
 	$(INSTALL_BIN) -d $(MANDIR3)
 	$(INSTALL_BIN) -d $(MANDIR8)
-	$(INSTALL_DATA) man/*.1 $(MANDIR1)
 	$(INSTALL_DATA) man/*.3 $(MANDIR3)
 	$(INSTALL_DATA) man/*.8 $(MANDIR8)
 
