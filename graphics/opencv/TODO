- test on other arch. Just tested on amd64
- check SHARED_LIBS number, should be ++? But how 0.1 or 1.0?
- (just) first extract fails because OpenCV-2.3.1 create an ugly
  OpenCV-2.3.1/./* dot sub-folder. I think this dot-folder is a 
  mistake. I hope it will fit it. (Bug-Ticket is out). 
