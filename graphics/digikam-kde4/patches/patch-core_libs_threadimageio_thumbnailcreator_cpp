$OpenBSD$
Disable usage of KDCraw until crash reasons are determined.
--- core/libs/threadimageio/thumbnailcreator.cpp.orig	Sun Mar 31 23:25:40 2013
+++ core/libs/threadimageio/thumbnailcreator.cpp	Mon Apr  1 14:18:42 2013
@@ -499,20 +499,20 @@ ThumbnailImage ThumbnailCreator::createThumbnail(const
     }
 
     // Trying to load with dcraw: RAW files.
-    if (qimage.isNull())
-    {
-        if (KDcraw::loadEmbeddedPreview(qimage, path))
-        {
-            fromEmbeddedPreview = true;
-            profile             = metadata.getIccProfile();
-        }
-    }
-
-    if (qimage.isNull())
-    {
-        //TODO: Use DImg based loader instead?
-        KDcraw::loadHalfPreview(qimage, path);
-    }
+//    if (qimage.isNull())
+//    {
+//        if (KDcraw::loadEmbeddedPreview(qimage, path))
+//        {
+//            fromEmbeddedPreview = true;
+//            profile             = metadata.getIccProfile();
+//        }
+//    }
+//
+//    if (qimage.isNull())
+//    {
+//        //TODO: Use DImg based loader instead?
+//        KDcraw::loadHalfPreview(qimage, path);
+//    }
 
     // DImg-dependent loading methods: TIFF, PNG, everything supported by QImage
     if (qimage.isNull() && !failedAtDImg)
