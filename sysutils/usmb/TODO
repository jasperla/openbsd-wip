bad error handling, e.g. double free if the mountpoint does not exist

directory listings work :)

first access results in "no talloc stackframe around, leaking memory"

trying to read a file:
$ cat foo.txt                                 
assertion "32768 >= len" failed: file "/usr/obj/usmb-20100212/usmb-20100212/usmb_file.c", line 150, function "usmb_read"


