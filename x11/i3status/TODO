- some basic functionality works, but a lot still needs to be ported
  to OpenBSD, at least there is some FreeBSD to work with already.

* disk usage calculation doesn't work correctly, the numbers are off by quite a bit
