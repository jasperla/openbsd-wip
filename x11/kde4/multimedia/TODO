KMix does not build, should be ported to our mixer(4).

Dragon is broken (but it was playing something for me before sometimes,
so this is a regression too - grayed).

   * TunePimp  <http://www.musicbrainz.org/products/tunepimp>
     A library for developing MusicBrainz enabled tagging applications
     Provides MusicBrainz tagging in Juk.
