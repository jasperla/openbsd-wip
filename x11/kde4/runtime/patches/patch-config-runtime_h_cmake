$OpenBSD$
--- config-runtime.h.cmake.orig	Mon Oct 15 18:05:41 2012
+++ config-runtime.h.cmake	Mon Oct 15 18:06:38 2012
@@ -39,6 +39,9 @@
 /* Number of bits in a file offset, on hosts where this is settable. */
 #define _FILE_OFFSET_BITS 64
 
+/* Define to 1 if ALSA was found and we're allowed to use it. */
+#cmakedefine ALSA_FOUND
+
 /*
  * On HP-UX, the declaration of vsnprintf() is needed every time !
  */
