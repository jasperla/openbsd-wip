R backend doesn't work: libRblas and libRlapack are not resolved,
didn't find any way better than setting LD_LIBRARY_PATH to
"${LOCALBASE}/lib/R/lib".

Needs R to be overhauled. Need to pick up the slack.
