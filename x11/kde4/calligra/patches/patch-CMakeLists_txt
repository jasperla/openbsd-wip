$OpenBSD$
The "-m" option is not supported on non-GNU platforms.
--- CMakeLists.txt.orig	Fri Jan 20 02:21:09 2012
+++ CMakeLists.txt	Fri Jan 20 02:32:57 2012
@@ -260,7 +260,7 @@ if( MARBLE_FOUND )
     ##
     find_file(MARBLECONTROLBOX_H MarbleControlBox.h ${MARBLE_INCLUDE_DIR})
     if( MARBLECONTROLBOX_H )
-        execute_process(COMMAND grep -m 1 -e "setMarbleWidget" ${MARBLECONTROLBOX_H}
+        execute_process(COMMAND grep -e "setMarbleWidget" ${MARBLECONTROLBOX_H}
                         OUTPUT_VARIABLE SETMARBLEWIDGET
         )
         if( SETMARBLEWIDGET )
