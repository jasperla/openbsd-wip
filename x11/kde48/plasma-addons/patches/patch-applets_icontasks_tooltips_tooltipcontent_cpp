$OpenBSD$
--- applets/icontasks/tooltips/tooltipcontent.cpp.orig	Thu Nov 17 14:58:05 2011
+++ applets/icontasks/tooltips/tooltipcontent.cpp	Sun Dec 11 20:05:35 2011
@@ -189,12 +189,12 @@ void ToolTipContent::setWindowsToPreview(const QList<W
 
 QList<WId> ToolTipContent::windowsToPreview() const
 {
-    QList<WId> w;
+    QList<WId> windows;
 
-    foreach (Window d, d->windowsToPreview) {
-        w.append(d.id);
+    foreach (Window w, d->windowsToPreview) {
+        windows.append(w.id);
     }
-    return w;
+    return windows;
 }
 
 void ToolTipContent::setPlayState(const QString &ps)
