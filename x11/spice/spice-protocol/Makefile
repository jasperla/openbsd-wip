# $OpenBSD$

COMMENT=	headers defining SPICE protocols
DISTNAME=	spice-protocol-0.10.1
MASTER_SITES=   ${MASTER_SITE_SPICE:=releases/}

# BSD
PERMIT_PACKAGE_CDROM=	Yes
PERMIT_PACKAGE_FTP=	Yes
PERMIT_DISTFILES_CDROM=	Yes
PERMIT_DISTFILES_FTP=	Yes

CONFIGURE_STYLE= gnu

.include <bsd.port.mk>
