# $OpenBSD$

     SUBDIR =
     SUBDIR += asm
     SUBDIR += browser
     SUBDIR += c99
     SUBDIR += configure
     SUBDIR += cpp
     SUBDIR += editor
     SUBDIR += libdesktop
     SUBDIR += libsystem
     SUBDIR += panel
     SUBDIR += pdfviewer
     SUBDIR += surfer

.include <bsd.port.subdir.mk>
