$OpenBSD$

Index: xpra/buffers/memalign.c
--- xpra/buffers/memalign.c.orig
+++ xpra/buffers/memalign.c
@@ -14,7 +14,7 @@
 #define _STDINT_H
 #endif
 #if !defined(__APPLE__) && !defined(__FreeBSD__) && !defined(__DragonFly__)
-#include <malloc.h>
+#include <stdlib.h>
 #endif
 
 #ifdef __cplusplus
