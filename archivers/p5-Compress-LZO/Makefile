# $OpenBSD$

COMMENT =		reads/writes lzo files/buffers

DISTNAME =		Compress-LZO-1.08

CATEGORIES =		archivers

MAINTAINER =		Todd T. Fries <todd@OpenBSD.org>

# Perl
PERMIT_PACKAGE_CDROM =	Yes

MODULES =		cpan
BUILD_DEPENDS =		${RUN_DEPENDS}
RUN_DEPENDS =		archivers/lzo

.include <bsd.port.mk>
