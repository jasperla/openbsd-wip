$OpenBSD$

Index: static-deps/luajit/Makefile
--- static-deps/luajit/Makefile.orig
+++ static-deps/luajit/Makefile
@@ -110,7 +110,7 @@ INSTALL_DEP= src/luajit
 
 default all $(INSTALL_DEP):
 	@echo "==== Building LuaJIT $(VERSION) ===="
-	$(MAKE) -C src
+	$(MAKE) -C src CC=${CC} CCOPT=${CCOPT} CCOPT_x86=${CCOPT_x86} Q=${Q}
 	@echo "==== Successfully built LuaJIT $(VERSION) ===="
 
 install: $(INSTALL_DEP)
