$OpenBSD$

Index: third-party/cmake/BuildLuajit.cmake
--- third-party/cmake/BuildLuajit.cmake.orig
+++ third-party/cmake/BuildLuajit.cmake
@@ -19,16 +19,6 @@ function(BuildLuajit)
 
   ExternalProject_Add(${_luajit_TARGET}
     PREFIX ${DEPS_BUILD_DIR}
-    URL ${LUAJIT_URL}
-    DOWNLOAD_DIR ${DEPS_DOWNLOAD_DIR}/luajit
-    DOWNLOAD_COMMAND ${CMAKE_COMMAND}
-      -DPREFIX=${DEPS_BUILD_DIR}
-      -DDOWNLOAD_DIR=${DEPS_DOWNLOAD_DIR}/luajit
-      -DURL=${LUAJIT_URL}
-      -DEXPECTED_SHA256=${LUAJIT_SHA256}
-      -DTARGET=${_luajit_TARGET}
-      -DUSE_EXISTING_SRC_DIR=${USE_EXISTING_SRC_DIR}
-      -P ${CMAKE_CURRENT_SOURCE_DIR}/cmake/DownloadAndExtractFile.cmake
     CONFIGURE_COMMAND "${_luajit_CONFIGURE_COMMAND}"
     BUILD_IN_SOURCE 1
     BUILD_COMMAND "${_luajit_BUILD_COMMAND}"
@@ -92,6 +82,7 @@ elseif(MINGW AND CMAKE_CROSSCOMPILING)
     # Similar to Unix + cross - fPIC
     INSTALL_COMMAND
       ${MAKE_PRG} PREFIX=${DEPS_INSTALL_DIR}
+	SOURCE_DIR=${DEPS_BUILD_DIR}/luajit
         BUILDMODE=static install
         TARGET_SYS=${CMAKE_SYSTEM_NAME}
         CROSS=${CROSS_TARGET}-
