COMMENT=		BitStamp API and IPN calls

MODULES=		cpan
VER=			0.02
DISTNAME=		Finance-BitStamp-API-${VER}
PKGNAME=		p5-${DISTNAME}
CATEGORIES=		misc perl5
CPAN_AUTHOR=		PEASWORTH

MAINTAINER=		Todd T. Fries <todd@fries.net>

# Artistic
PERMIT_PACKAGE=     Yes
PERMIT_DISTFILES=   Yes

#RUN_DEPENDS+= ${BUILD_DEPENDS}
#BUILD_DEPENDS+= 

CONFIGURE_STYLE= perl

.include <bsd.port.mk>
