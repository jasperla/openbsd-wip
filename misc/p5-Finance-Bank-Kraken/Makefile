COMMENT=		api.kraken.com connector

MODULES=		cpan
VER=			0.3
DISTNAME=		Finance-Bank-Kraken-${VER}
PKGNAME=		p5-${DISTNAME}
CATEGORIES=		misc perl5
CPAN_AUTHOR=		PHILIPPE

MAINTAINER=		Todd T. Fries <todd@fries.net>

# Artistic
PERMIT_PACKAGE=     Yes
PERMIT_DISTFILES=   Yes

#RUN_DEPENDS+= ${BUILD_DEPENDS}
#BUILD_DEPENDS+= 

CONFIGURE_STYLE= perl

.include <bsd.port.mk>
