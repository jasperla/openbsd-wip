$OpenBSD$

https://github.com/nim-lang/Nim/issues/3506

--- koch.nim.orig	Tue Dec 22 05:03:13 2015
+++ koch.nim	Tue Dec 22 05:04:19 2015
@@ -339,7 +339,7 @@ proc tests(args: string) =
   exec "nim cc --taintMode:on tests/testament/tester"
   # Since tests take a long time (on my machine), and we want to defy Murhpys
   # law - lets make sure the compiler really is freshly compiled!
-  exec "nim c --lib:lib -d:release --opt:speed compiler/nim.nim"
+  exec "nim c --lib:lib -d:release --opt:speed -p:. -p:lib/packages/docutils compiler/nim.nim"
   let tester = quoteShell(getCurrentDir() / "tests/testament/tester".exe)
   let success = tryExec tester & " " & (args|"all")
   if not existsEnv("TRAVIS") and not existsEnv("APPVEYOR"):
