$OpenBSD$
--- src/jemalloc/configure.orig	Sun Jan 25 14:12:58 2015
+++ src/jemalloc/configure	Sun Jan 25 14:13:15 2015
@@ -4879,7 +4879,13 @@ case "${host}" in
 	CFLAGS="$CFLAGS"
 	abi="elf"
 	$as_echo "#define JEMALLOC_PURGE_MADVISE_FREE  " >>confdefs.h
+	;;
+  *-*-openbsd*)
+	CFLAGS="$CFLAGS"
+	abi="elf"
+	$as_echo "#define JEMALLOC_PURGE_MADVISE_FREE  " >>confdefs.h
 
+	force_tls="0"
 	;;
   *-*-linux*)
 	CFLAGS="$CFLAGS"
