$OpenBSD$
ksh parsing problem
--- configure.orig	Sun Mar  8 06:53:20 2015
+++ configure	Sun Mar  8 06:53:34 2015
@@ -698,7 +698,7 @@ probe CFG_ADB        adb
 if [ ! -z "$CFG_PANDOC" ]
 then
     PV_MAJOR_MINOR=$(pandoc --version | grep '^pandoc' |
-        # Extract "MAJOR MINOR" from Pandoc's version number
+        # Extract "MAJOR MINOR" from Pandoc s version number
         sed -E 's/pandoc(.exe)? ([0-9]+)\.([0-9]+).*/\2 \3/')
 
     MIN_PV_MAJOR="1"
