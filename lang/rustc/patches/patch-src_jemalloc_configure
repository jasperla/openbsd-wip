$OpenBSD$
--- src/jemalloc/configure.orig	Thu Jan 22 17:10:06 2015
+++ src/jemalloc/configure	Thu Jan 29 08:35:51 2015
@@ -4879,7 +4879,13 @@ case "${host}" in
 	CFLAGS="$CFLAGS"
 	abi="elf"
 	$as_echo "#define JEMALLOC_PURGE_MADVISE_FREE  " >>confdefs.h
+	;;
+  *-*-openbsd*)
+	CFLAGS="$CFLAGS"
+	abi="elf"
+	$as_echo "#define JEMALLOC_PURGE_MADVISE_FREE  " >>confdefs.h
 
+	force_tls="0"
 	;;
   *-*-linux*)
 	CFLAGS="$CFLAGS"
