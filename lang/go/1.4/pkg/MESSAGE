If you want to use this package as your default system go, as root
create symbolic links like so (overwriting any previous default):
 ln -sf ${PREFIX}/go/1.4/bin/go ${PREFIX}/bin/go
 ln -sf ${PREFIX}/go/1.4/bin/gofmt ${PREFIX}/bin/gofmt
