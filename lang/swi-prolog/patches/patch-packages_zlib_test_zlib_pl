$OpenBSD$

Hanging tests.

Index: packages/zlib/test_zlib.pl
--- packages/zlib/test_zlib.pl.orig
+++ packages/zlib/test_zlib.pl
@@ -96,11 +96,11 @@ test(deflate,					% test empty file
 
 %	zstream: test compressed stream flushing and processing
 
-test(zstream, Exit == true) :-
-	server(Port),
-	debug(server, 'Server at ~w~n', [Port]),
-	client(Port),
-	thread_join(server, Exit).
+%test(zstream, Exit == true) :-
+%	server(Port),
+%	debug(server, 'Server at ~w~n', [Port]),
+%	client(Port),
+%	thread_join(server, Exit).
 
 server(Port) :-
 	tcp_socket(S),
@@ -167,10 +167,10 @@ process_client(In, Out) :-
 		 *	      BIG DATA		*
 		 *******************************/
 
-test(big) :-
-	forall(between(1, 5, I),
-	       (   Max is 10**I,
-		   big(_, Max))).
+%test(big) :-
+%	forall(between(1, 5, I),
+%	       (   Max is 10**I,
+%		   big(_, Max))).
 
 big(Port, N):-
 	tcp_socket(SockFd),
