Several issues with the port:

  * Libraries from "pkg/PFRAG.shared-main" are not treated as libraries
    (suffixes).
  * Architectures.  Tested only on amd64.  Probably gcc-from-ports dependency
    may be patched away.  Didn't look into that.
